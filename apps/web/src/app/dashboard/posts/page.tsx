import { Box, Tabs, TabsList, TabsTab, TabsPanel } from "@mantine/core";

const PostsPage = async () => {
  return <TabsPanel value="gallery">Gallery tab content</TabsPanel>;
};

export default PostsPage;
