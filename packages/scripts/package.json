{
  "name": "@repo/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "dev": "tsc --project tsconfig.build.json --watch",
    "gen-token": "dotenv -e .env -- tsx src/generate-invitation-token.ts",
    "test-token": "dotenv -e .env -- tsx src/test-invitation-token.ts",
    "test-validate": "dotenv -e .env -- tsx src/test-validate-token.ts",
    "list-tokens": "dotenv -e .env -- tsx src/list-invitation-tokens.ts"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/services": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22.13.8",
    "dotenv-cli": "^8.0.0",
    "tsx": "^4.20.3"
  }
}
