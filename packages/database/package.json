{
  "name": "database",
  "version": "1.0.0",
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "db:push": "npx prisma db push --accept-data-loss",
    "db:generate": "npx prisma generate",
    "build": "tsc",
    "dev": "tsc -w"
  },
  "devDependencies": {
    "prisma": "^5.16.1"
  },
  "dependencies": {
    "@prisma/client": "5.16.1",
    "typescript": "^5.1.3"
  }
}
